"# Portfolio-website"

## Local Development

- Run `npm run dev` to start Next.js frontend + `/api/contact` backend together.
- Run `npm run build` for production build.
- Run `npm run start` to serve production build locally.
